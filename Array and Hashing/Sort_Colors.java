import java.util.*;

class Solution {
    public void sortColors(int[] nums) {
        // TC: O(n log n)
        // SC: O(1) if we consider the input array as the output
        Arrays.sort(nums);
    }
}